<div class="text-center">
    <label for="{{$value}}" class="rounded-full bg-white">
        <input type="{{$tipus}}" id="{{$value}}" name="{{$nom}}" value="{{$value}}" class="checkProva">
        <img src="{{$src}}" width="70px" name="{{$value}}" class="imatgePlanificacio">
    </label>
    <span class="text-sm">{{$span}}</span>
</div>
